# =============================================================================
# secrets.yaml - Template
# =============================================================================
# Copier ce fichier en `secrets.yaml` et remplacer les valeurs ci-dessous par
# tes propres credentials. Le fichier `secrets.yaml` est dans .gitignore et
# ne doit JAMAIS etre commit.
#
# Generer une nouvelle cle API ESPHome :
#   python -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
# =============================================================================

# WiFi du reseau principal
wifi_ssid: "MonReseauWiFi"
wifi_password: "MotDePasseWiFi"

# AP fallback (active si le WiFi principal echoue)
fallback_password: "fallback123"

# Cle de chiffrement Noise Protocol (32 octets base64)
api_encryption_key: "RemplaceMoiParUneCleNoise32BytesBase64=="

# Mot de passe OTA (port 3232)
ota_password: "RemplaceMoiParUnMotDePasseOTA"
